import { DietfilterPipe } from './dietfilter.pipe';

describe('DietfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DietfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
