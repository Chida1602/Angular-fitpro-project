import { CustomdDirective } from './customd.directive';

describe('CustomdDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomdDirective();
    expect(directive).toBeTruthy();
  });
});
